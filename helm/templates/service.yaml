apiVersion: v1
kind: Service
metadata:
  name: {{ include "ucl-team-simulator.fullname" . }}
  labels:
    {{- include "ucl-team-simulator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ucl-team-simulator.selectorLabels" . | nindent 4 }}
